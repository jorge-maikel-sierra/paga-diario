<%- include('../../layouts/header') %>

<div class="p-4 sm:p-6 lg:p-8 space-y-6">

  <!-- =========================================
       ENCABEZADO
  ========================================= -->
  <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
    <div>
      <h1 class="text-2xl font-bold text-ink">Gestión de Rutas</h1>
      <p class="text-sm text-ink-secondary mt-1">
        <%= routes.length %> ruta<%= routes.length !== 1 ? 's' : '' %> registrada<%= routes.length !== 1 ? 's' : '' %>
      </p>
    </div>
    <a href="/admin/routes/new"
      class="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-brand-green text-white
             text-sm font-semibold shadow-sm hover:bg-green-500 transition-colors whitespace-nowrap">
      + Nueva Ruta
    </a>
  </div>

  <!-- =========================================
       ESTADO VACÍO
  ========================================= -->
  <% if (routes.length === 0) { %>
    <div class="bg-surface rounded-xl border border-line shadow-sm py-20 text-center">
      <span class="text-5xl block mb-4">🗺️</span>
      <p class="text-base font-semibold text-ink-secondary">No hay rutas registradas</p>
      <p class="text-sm text-ink-muted mt-1">Crea la primera ruta para comenzar a organizar los cobros.</p>
      <a href="/admin/routes/new"
        class="mt-5 inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-brand-green
               text-white text-sm font-semibold hover:bg-green-500 transition-colors">
        + Nueva Ruta
      </a>
    </div>

  <% } else { %>

  <!-- =========================================
       TABLA DE RUTAS
  ========================================= -->
  <div class="bg-surface rounded-xl border border-line shadow-sm overflow-x-auto">
    <table class="min-w-full text-sm">
      <thead class="bg-app border-b border-line">
        <tr>
          <th class="px-5 py-3 text-left text-xs font-semibold text-ink-secondary uppercase tracking-wide">
            Nombre de la Ruta
          </th>
          <th class="px-5 py-3 text-left text-xs font-semibold text-ink-secondary uppercase tracking-wide">
            Descripción
          </th>
          <th class="px-5 py-3 text-left text-xs font-semibold text-ink-secondary uppercase tracking-wide">
            Cobrador asignado
          </th>
          <th class="px-5 py-3 text-center text-xs font-semibold text-ink-secondary uppercase tracking-wide">
            Clientes
          </th>
          <th class="px-5 py-3 text-center text-xs font-semibold text-ink-secondary uppercase tracking-wide">
            Estado
          </th>
          <th class="px-5 py-3 text-right text-xs font-semibold text-ink-secondary uppercase tracking-wide">
            Acciones
          </th>
        </tr>
      </thead>
      <tbody class="divide-y divide-line-subtle">
        <% routes.forEach(function(route) { %>
          <tr class="hover:bg-surface-hover transition-colors">

            <!-- Nombre de la ruta -->
            <td class="px-5 py-4">
              <span class="font-semibold text-ink"><%= route.name %></span>
            </td>

            <!-- Descripción -->
            <td class="px-5 py-4 text-ink-secondary text-xs max-w-xs">
              <% if (route.description) { %>
                <%= route.description %>
              <% } else { %>
                <span class="text-ink-muted italic">—</span>
              <% } %>
            </td>

            <!-- Cobrador asignado -->
            <td class="px-5 py-4">
              <% if (route.collector) { %>
                <div class="flex items-center gap-2">
                  <div class="w-7 h-7 rounded-full bg-brand-navy flex items-center justify-center
                              text-white text-xs font-bold shrink-0">
                    <%= route.collector.firstName.charAt(0).toUpperCase() %><%= route.collector.lastName.charAt(0).toUpperCase() %>
                  </div>
                  <span class="text-sm font-medium text-ink">
                    <%= route.collector.firstName %> <%= route.collector.lastName %>
                  </span>
                </div>
              <% } else { %>
                <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full
                             text-xs font-semibold bg-warning/10 text-warning border border-warning/20">
                  ⚠ Sin cobrador asignado
                </span>
              <% } %>
            </td>

            <!-- Número de clientes -->
            <td class="px-5 py-4 text-center">
              <span class="inline-flex items-center justify-center w-8 h-8 rounded-full
                           bg-info/10 text-info text-xs font-bold">
                <%= route._count.clients %>
              </span>
            </td>

            <!-- Badge de estado -->
            <td class="px-5 py-4 text-center">
              <% if (route.isActive) { %>
                <span class="inline-flex items-center gap-1 px-2.5 py-0.5 rounded-full
                             text-xs font-semibold bg-green-100 text-green-700">
                  ● Activa
                </span>
              <% } else { %>
                <span class="inline-flex items-center gap-1 px-2.5 py-0.5 rounded-full
                             text-xs font-semibold bg-surface-hover text-ink-secondary">
                  ● Inactiva
                </span>
              <% } %>
            </td>

            <!-- Acciones -->
            <td class="px-5 py-4">
              <div class="flex items-center justify-end gap-2">
                <a href="/admin/routes/<%= route.id %>"
                  class="inline-flex items-center gap-1 px-3 py-1.5 rounded-lg
                         border border-brand-green text-brand-green text-xs font-semibold
                         hover:bg-brand-green hover:text-white transition-colors whitespace-nowrap">
                  📍 Ver Clientes / Mapa
                </a>
                <a href="/admin/routes/<%= route.id %>/edit"
                  class="inline-flex items-center gap-1 px-3 py-1.5 rounded-lg
                         border border-line text-ink-secondary text-xs font-semibold
                         hover:bg-surface-hover transition-colors">
                  ✏️ Editar
                </a>
              </div>
            </td>

          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>

  <% } %>

</div>

<%- include('../../layouts/footer') %>
